# Local Terraform directories
.terraform/
.terraform.lock.hcl

# Terraform state files
*.tfstate
*.tfstate.*
terraform.tfstate.backup

# Crash log files
crash.log

# Exclude all .tfvars files, which might contain sensitive data, such as
# password, secret keys, etc. You can also ignore specific .tfvars files
# if you prefer to include some .tfvars that do not contain sensitive information.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally and
# should not be shared in version control
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore CLI configuration files
.terraformrc
terraform.rc
